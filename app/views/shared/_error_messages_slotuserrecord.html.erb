<% if @slotuserrecord.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@slotuserrecord.errors.count, "error") %>.
    </div>
    <ul>
    <% @slotuserrecord.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>